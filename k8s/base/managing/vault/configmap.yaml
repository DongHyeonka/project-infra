apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-config
  namespace: mnt
data:
  VAULT_LOCAL_CONFIG: |
    storage "file" {
      path = "/vault/data"
    }
    listener "tcp" {
      address     = "0.0.0.0:8200"
      tls_disable = 1
    }
    ui = true
