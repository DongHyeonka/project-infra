apiVersion: v1
kind: Service
metadata:
  name: vault-ui
  namespace: mnt
spec:
  type: NodePort
  selector:
    app: vault
  ports:
    - name: ui
      port: 8200
      targetPort: 8200
      nodePort: 30820
