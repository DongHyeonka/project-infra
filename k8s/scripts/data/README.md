# Data Operations

Data scripts are for imperative data-plane operations.

Examples:
- run a manual backup trigger
- check stateful workload health
- execute emergency restore steps

Declarative stateful workload resources belong under `base/app/units/<unit>/<domain>/stateful/<workload>/`.
